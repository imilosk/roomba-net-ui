import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yourcompany.roombanet',
  appName: 'roomba-net-ui',
  webDir: 'dist'
};

export default config;
